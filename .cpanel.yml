deployment:
  tasks:
    - export NODE_ENV=production
    - npm install
    - npm run build
    - /bin/rm -rf /home/technosh/dev.technoshineph.com/*
    - /bin/cp -r dist/* /home/technosh/dev.technoshineph.com/
    - /bin/cp .htaccess /home/technosh/dev.technoshineph.com/ || true